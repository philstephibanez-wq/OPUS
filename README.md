# OPUS FRAMEWORK

Multi-site MVC framework driven by finite-state machines.

The repository root contains the framework, Composer dependencies and OPUS sites.
Each site owns its configuration, runtime storage and public entry point.
