# P112Q3E2 Patch

Apply to `H:\ASAP`.

This patch changes ACL PHP source files only to add metadata attributes and the `RefBookInspectableInterface` opt-in where applicable. Runtime ACL behavior is unchanged.

Generated reports under `var/refbook` and `var/reports` are intentionally not included.
