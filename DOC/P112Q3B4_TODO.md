# P112Q3B4 TODO

- Validate Mailpit `HTML` rendering and compare `HTML Check` warning count.
- Validate visible form section in the opened browser report.
- Keep Panther as `SKIPPED` until the real local Panther autoload is identified.
- Later: add a true browser-driven form submission once Panther is available and stable.
