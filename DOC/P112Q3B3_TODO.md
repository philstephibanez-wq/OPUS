# P112Q3B3 TODO

- If Panther is expected to be mandatory, confirm where `Symfony\Component\Panther\Client` is installed and set `ASAP_P112Q3B2_PANTHER_AUTOLOAD` accordingly.
- After Panther is actually available, run the recipe with `ASAP_P112Q3B2_PANTHER_REQUIRED=1`.
- Later, connect this life recipe into the global evolutive recipe manifest if the manifest runner requires a central registration file.
