# P112Q3E TODO

1. Annotate critical ASAP domains progressively with `AsapRefBookClass` and `AsapRefBookMethod`.
2. Decide when strict RefBook attribute validation becomes blocking.
3. Build ASAP_REF_BOOK snapshot client.
4. Generate FSM Mermaid diagrams from real ASAP FSM data.
5. Generate Router/ACL/Security diagrams from real ASAP runtime contracts.
6. Add endpoint-level API when the snapshot model is stable.
