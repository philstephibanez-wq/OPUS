# TODO P7A0J_CLEAN_CLONE_I18N_SMTP_GATES

1. Apply the patch on `H:\OPUS`.
2. Run `RUN_P7A0J_CLEAN_CLONE_I18N_SMTP_GATES.cmd`.
3. If the smoke returns `P7A0J_EXIT_CODE=0`, commit with:
   `P7A0J_CLEAN_CLONE_I18N_SMTP_GATES`.
4. After commit, update `DOC/WORKSPACE_STATUS.md` in a small follow-up doc commit to mark P7A0J as validated.
5. Then continue to the next OPUS milestone: DB connection configuration contract + SQLite PDO model smoke, unless a blocker appears first.
